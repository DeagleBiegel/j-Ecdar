test { 

assert(((L3L1==false) &&  cas.d- cas.c<=0 &&  cas.g- cas.c<=0 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(!cas.armed && !cas.locked && !cas.closed && !cas.sound && !cas.flash);
assert(((L3L1==false) &&  cas.d- cas.c<=0 &&  cas.g- cas.c<=0 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
cas.close();
assert(((L3L1==false) &&  cas.d- cas.c<=0 &&  cas.g- cas.c<=0 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(!cas.armed && !cas.locked && cas.closed && !cas.sound && !cas.flash);
assert(((L3L1==false) &&  cas.d- cas.c<=0 &&  cas.g- cas.c<=0 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
cas.lock();
c = 0;
assert(((L3L1==false) &&  cas.c<=2 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(!cas.armed && cas.locked && cas.closed && !cas.sound && !cas.flash);
assert(((L3L1==false) &&  cas.c<=2 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
cas.unlock();
assert(((L3L1==true) &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(!cas.armed && !cas.locked && cas.closed && !cas.sound && !cas.flash);
assert(((L3L1==true) &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
cas.lock();
c = 0;
assert(((L3L1==true) &&  cas.c<=2 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(!cas.armed && cas.locked && cas.closed && !cas.sound && !cas.flash);
assert(((L3L1==true) &&  cas.c<=2 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
cas.wait(2);
assert(((L3L1==true) &&  cas.c>=2 &&  cas.d>=2 &&  cas.g>=2 &&  cas.c- cas.d<=0 &&  cas.g- cas.d<=0 &&  cas.c- cas.g<=0 &&  cas.d- cas.g<=0));
assert(cas.armed && cas.locked && cas.closed && !cas.sound && !cas.flash);
}

